import 'package:figma/src/utils/library.dart';

class AppClearPage extends StatelessWidget {
  const AppClearPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
